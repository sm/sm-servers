#!/bin/sh

if (( ${_pid} > 0 ))
then kill -USR1 ${_pid}
    if (($? == 0 ))
    then rm -f /var/lock/subsys/haproxy /var/run/haproxy/haproxy.pid
    fi
else
    echo "It appears that haproxy is no longer running (no pidfile)."
fi
